# Copy to .env locally. Never commit secrets.
# YouTube Data API OAuth desktop client JSON (from Google Cloud Console):
# GOOGLE_CLIENT_SECRETS=./client_secret.json
# Token is created on first upload:
# YOUTUBE_TOKEN=./token.json
